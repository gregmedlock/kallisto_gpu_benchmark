[project]
name = "kallisto-gpu-benchmark"
version = "0.1.0"
description = "Cost-normalized benchmark of GPU-kallisto vs CPU kallisto across EC2 instance types"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "pandas>=3.0.1",
]
